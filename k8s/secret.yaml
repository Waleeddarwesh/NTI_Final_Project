---
# k8s/secret.yaml
# TEMPLATE ONLY — base64-encoded placeholder values.
# Real values are populated by the Jenkinsfile (Phase 6) which reads from
# AWS Secrets Manager and runs:
#   kubectl create secret generic nti-devops-secret \
#     --from-literal=SECRET_KEY="$(aws secretsmanager get-secret-value ...)" \
#     --from-literal=DATABASE_URL="postgres://..." \
#     --from-literal=REDIS_URL="redis://..." \
#     --namespace nti-devops \
#     --save-config --dry-run=client -o yaml | kubectl apply -f -
#
# DO NOT commit this file with real base64-encoded secrets.
# The placeholder values below are the string "REPLACE_ME" base64-encoded.

apiVersion: v1
kind: Secret
metadata:
  name: nti-devops-secret
  namespace: nti-devops
  labels:
    app: nti-devops
    component: secret
type: Opaque
data:
  # echo -n "your-value" | base64
  SECRET_KEY: UkVQTEFDRV9NRQ==
  DATABASE_URL: UkVQTEFDRV9NRQ==
  REDIS_URL: UkVQTEFDRV9NRQ==
  DB_PASSWORD: UkVQTEFDRV9NRQ==
